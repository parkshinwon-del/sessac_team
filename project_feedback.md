# 프로젝트 피드백

## [형우]

### 1. 이해관계자와 비즈니스 목표

딥러닝을 배우고 이를 활용할 수 있는 미니 프로젝트를 진행해보고자 물색하던 중, AI Hub에서 음식 이미지를 가지고 분류 및 양 추정을 해보는 것이 이해하기 좋을 것 같아서 시작하게 되었다. 별도의 외부 이해관계자는 없었고, 팀원 3명이 실습 목적의 학습 프로젝트로 진행했다.

### 2. 최종 결과

음식 분류용 데이터셋은 종류가 다양했던 반면, 양 추정 데이터셋은 종류가 한정적이어서 밥류(14종)로 범위를 좁혀 진행했다. 신원님과 민선님이 전처리를 꼼꼼하게 해주신 덕분에, 1.5일 작업치고는 Streamlit 데모에서 음식 분류와 양 추정 모두 체감상 나쁘지 않은 성능을 보여줬다. (양추정 val_acc 최종 0.9383)

### 3. 사용한 기술

| 영역 | 기술 |
|---|---|
| 양 추정 | PyTorch, torchvision (ResNet18 Fine-tuning + MLP Head) |
| 데이터 처리 | Pandas (labels.csv 가공), Pillow(PIL) (이미지 전처리) |
| 모델 진단 | Grad-CAM — 모델이 이미지의 어느 부분에 반응하는지 시각화 |
| 서빙/UI | Streamlit (로컬 웹 데모) |
| 데이터셋 | AI Hub "음식 이미지 및 영양정보 텍스트" |
| 협업/버전관리 | GitHub |
| 개발 환경 | Conda (가상환경 분리) |

양추정은 ResNet18로 이미지 특징을 추출한 뒤 MLP로 Q등급을 분류하는 구조로 시작했고, 이후 ResNet도 함께 학습시키는 Fine-tuning 방식으로 전환해 정확도를 크게 끌어올렸다. UI는 Streamlit으로 사진 업로드 → 결과 출력까지 이어지는 간단한 웹 데모를 만들었다.

### 4. 가장 큰 리스크와 극복 방법

양 추정 모델의 원천 데이터가 스튜디오에서 통제된 조건(고정 카메라 거리, 단일 음식, 균일한 배경)으로 촬영된 사진이었다는 점이 가장 큰 리스크였다. 이 때문에 모델이 "실제 음식 양"이 아니라 "사진에서 음식이 차지하는 확대 비율"에 의존해 판단하는 경향(지름길 학습)이 생겼고, 실제 사용자가 자유롭게 찍은 사진(다른 거리, 여러 음식이 섞인 복잡한 구도)에서는 예측이 크게 흔들리는 문제를 발견했다.

이 문제는 Grad-CAM으로 모델이 이미지의 어느 부분을 보고 판단하는지 직접 시각화해 진단했고, 학습 데이터에 RandomResizedCrop 같은 확대/축소 증강을 추가해 모델이 절대적인 크기에 덜 의존하도록 완화를 시도했다. 다만 근본적으로는 학습 데이터 자체가 단일 음식·통제된 구도 위주라 완전히 해결하지는 못했고, 이 부분은 README.md에 "발견한 한계"로 문서화해 남겨두는 선에서 마무리했다.

### 5. 다시 시작한다면 다르게 할 점

- **시작 문서 작성**: 문서 작성이 초반엔 부담스러울 수 있지만, 상세한 파이프라인 설계도와 폴더/파일 배치 규칙을 미리 논의하고, GitHub 사용법(브랜치 전략, 커밋 컨벤션 등)을 팀 전체가 숙지한 뒤 시작했다면 중간에 겪은 경로/인코딩/환경 문제들을 훨씬 줄일 수 있었을 것 같다.
- **다양한 추론 모델 비교**: ResNet 특징 추출 후 MLP로 학습하는 방식으로 시작했는데, 민선님이 시도한 Fine-tuning 방식이 정확도를 훨씬 크게 끌어올렸다. 처음부터 여러 방식을 병렬로 실험해봤다면 더 빨리 좋은 방향을 찾았을 것 같다.
- **UI/UX 고도화**: 기능 구현에만 집중하다 보니 UI 관련 프롬프트를 상대적으로 대충 작성한 측면이 있다. 또한 실제 사용자가 웹으로 접근할 일은 많지 않을 텐데, 웹 전용으로만 설계한 것도 아쉬운 부분이다. 모바일 환경이나 실제 사용 맥락(예: 식사 전 바로 촬영)을 더 고려한 UX를 설계했다면 좋았을 것 같다.

---

## [신원]

### 1. 프로젝트 이해관계자와 목표

**1) 타깃**: 음식을 촬영해서 칼로리 정보를 알고 싶은 사용자들의 욕구를 해결해준다.

**2) 개발자**: 딥러닝 서비스를 간단히 구현해본다.

### 2. 결과

14종류의 밥 종류를 분류할 수 있고, 양 추정을 통해 칼로리를 파악할 수 있다. 이를 바탕으로 개발자들의 1차 목표를 달성할 수 있었다.

### 3. 사용 기술

| 기술 | 사용 위치 | 역할 |
|---|---|---|
| Python | 전체 프로젝트 | 주 개발 언어 |
| YOLO | `models/yolo.py`, `Classification/predict.py` | 음식 객체 탐지 및 음식 종류 분류 |
| PyTorch | `main.py`, `Portion/*` | 양 추정 신경망 학습·추론 |
| ResNet18 | `Portion/portion_estimate.py` | 음식 이미지 특징 추출 |
| MLP | `Portion/estimate.py`, `portion_estimate.py` | 이미지 특징 + 음식 종류를 이용해 Q1~Q5 양 등급 예측 |
| Streamlit | `app/streamlit_app.py` | 사용자가 사진을 업로드하고 결과를 확인하는 웹 UI |

### 4. 가장 큰 리스크

전처리 과정에서 일부 음식 클래스의 라벨이 제대로 생성되지 않아 원인을 파악하는 일이 가장 힘들었다. 이에 LLM에게 문제 상황에 관한 사진과 글을 충분히 주어 문제 상황 개선 코드를 만들 수 있었다.

밥에 대한 양 추정 데이터 종류가 14가지 정도밖에 주어지지 않은 것도 한계였다.

### 5. 다시 시작할 시 차별화 포인트: 범용성과 편의성 확보, 그리고 근본적인 시장조사

- 음식의 종류를 늘려야 한다. 14종류의 음식 추론은 실제로 써먹는 상황이 나오기 매우 힘들다.
- 모바일 연동이 되게 해야 한다. 현재는 쓰기 너무 불편하다.
- 시장 조사를 더 많이 해야 한다. 이미 훨씬 나은 기능의 서비스가 많으므로, 차별화 포인트를 늘려야 한다.

---

## [민선]

### 1. 이해관계자와 프로젝트 목표

딥러닝 파이프라인을 처음부터 끝까지 직접 구현하고, 특히 모델 성능을 개선하는 과정 자체를 경험해보고 싶어서 참여했다. 별도의 외부 이해관계자는 없었고, 팀 내에서는 양 추정 모델의 정확도를 실제로 쓸 만한 수준까지 끌어올리는 것이 목표였다.

### 2. 최종 결과

처음엔 ResNet18을 고정하고 특징만 뽑아 MLP로 학습하는 방식(Feature Extraction)으로 시작했는데, val_acc가 최고 0.63 정도에서 정체됐다. ResNet도 함께 학습시키는 Fine-tuning 방식으로 바꾸자 val_acc가 0.9383까지 올라갔고, 팀 전체가 이 방식으로 최종 방향을 확정했다.

### 3. 사용한 기술

| 기술 | 사용처 | 역할 |
|---|---|---|
| PyTorch, torchvision | `Portion/portion_estimate.py` | ResNet18 Fine-tuning + MLP Head 구현 |
| ResNet18 | 양 추정 모델 백본 | 이미지 특징 추출 + 미세조정 |
| Grad-CAM | 진단용 | 모델이 이미지 어느 부분을 보고 판단하는지 시각화 |
| Data Augmentation | 학습 코드 내 transform | RandomResizedCrop 등으로 일반화 성능 개선 시도 |

이전 방식에서는 ResNet에서 특징만 추출하고 MLP로 학습했다면, Fine-tuning 방식에서는 ResNet도 함께 학습·미세조정해서 정확도가 크게 올라갔다. CNN이 ImageNet 특징이 아니라 "우리 음식 사진에 맞는 특징"을 새로 익힐 수 있었던 게 핵심이라고 생각한다.

### 4. 가장 큰 리스크와 극복 방법

Fine-tuning으로 val_acc가 0.94까지 오른 게 처음엔 반가웠는데, 모델이 "실제 양"이 아니라 "사진 속 음식이 얼마나 확대되어 찍혔는지"에 의존해서 판단하는 경향(지름길 학습)을 발견했다. 학습 데이터가 스튜디오 촬영이라 카메라 거리가 거의 고정돼 있었던 게 원인이었다.

이 문제를 완화하려고 학습 데이터에 RandomResizedCrop 같은 확대/축소 증강을 추가했다. 다만 시간 관계상 "완전히 해결하지는 못한 한계"로 남았다.

### 5. 다시 시작한다면 다르게 할 점

- **처음부터 여러 방식을 병렬로 비교**: Feature Extraction으로 먼저 시간을 쓰고 나서야 Fine-tuning을 시도했는데, 시간이 촉박한 프로젝트일수록 처음부터 두 방식을 동시에 돌려서 더 빨리 방향을 잡았어야 했다.
- **검증 데이터 분할 방식을 더 꼼꼼히 확인**: train/val이 정확히 어떤 기준(같은 그릇 사진이 겹치지 않는지 등)으로 나뉘었는지 초반에 명확히 확인하지 않은 채 학습을 진행해서, 나온 정확도를 온전히 신뢰하기까지 시간이 걸렸다.
- **증강 효과를 수치로 검증**: RandomResizedCrop을 추가하긴 했지만, "추가 전/후 val_acc가 어떻게 바뀌었는지"를 표로 비교하는 작업까지는 못 했다. 다음엔 개선 시도마다 Before/After를 기록해두고 싶다.
